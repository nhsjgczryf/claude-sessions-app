node_modules/
dist/
out/
*.log
.DS_Store
.vscode/

# Web auth state — never commit credentials or session tokens
auth.json
auth-sessions.json

# Local session config — copy sessions.example.json on first run.
# Untracked so personal SSH hosts / paths never get pushed.
sessions.json
